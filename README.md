# PhoneShop - Интернет-магазин мобильных телефонов

## 📱 Описание проекта

PhoneShop - это полнофункциональный интернет-магазин мобильных телефонов и аксессуаров, разработанный на Django 5.2.7. Проект включает в себя все основные функции современного e-commerce сайта.

### 🎯 Основные возможности

- **🛍️ Каталог товаров** с фильтрацией, поиском и сортировкой
- **🛒 Корзина покупок** с AJAX-функциональностью
- **📦 Система заказов** в 3 шага
- **👤 Личный кабинет** с профилем пользователя
- **⭐ Система отзывов** и рейтингов
- **💝 Избранное** и сравнение товаров
- **🎁 Система скидок** и промокодов
- **📝 Блог** с новостями и статьями
- **📊 Административная панель** Django
- **🎨 Современный дизайн** с Bootstrap 5

## 🏗️ Технологический стек

- **Backend**: Django 5.2.7, Django REST Framework
- **Frontend**: Bootstrap 5, jQuery, Font Awesome
- **Database**: MySQL (поддержка SQLite для разработки)
- **Cache**: Redis
- **Storage**: Django Media Files
- **Authentication**: Django Auth с кастомными моделями

## 📋 Установка и настройка

### Предварительные требования

- Python 3.12+
- MySQL 8.0+ (опционально, для продакшена)
- Redis 6.0+ (опционально)

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd phone_shop
```

### 2. Создание виртуального окружения

```bash
# Создание виртуального окружения
python3 -m venv phone_shop_env

# Активация окружения (Linux/Mac)
source phone_shop_env/bin/activate

# Активация окружения (Windows)
phone_shop_env\Scripts\activate
```

### 3. Установка зависимостей

```bash
# Обновление pip
python -m pip install --upgrade pip

# Установка зависимостей из requirements.txt
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта на основе `.env.example`:

```bash
# База данных (для продакшена используйте MySQL)
DATABASE_URL=mysql://username:password@localhost:3306/phone_shop_db

# Настройки безопасности
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Email настройки (опционально)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
EMAIL_USE_TLS=True

# Redis настройки (опционально)
REDIS_URL=redis://127.0.0.1:6379/1
```

## 🗄️ Настройка базы данных

### Для разработки (SQLite)

База данных SQLite уже настроена по умолчанию. Просто запустите миграции:

```bash
python manage.py makemigrations
python manage.py migrate
```

### Для продакшена (MySQL)

1. Установите MySQL клиент:

```bash
sudo apt install mysql-client  # Linux
# или
pip install mysqlclient       # Через pip
```

2. Создайте базу данных:

```sql
CREATE DATABASE phone_shop_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'phone_shop_user'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON phone_shop_db.* TO 'phone_shop_user'@'localhost';
FLUSH PRIVILEGES;
```

## 👑 Создание суперпользователя

```bash
python manage.py createsuperuser
```

Следуйте инструкциям для создания административного аккаунта.

## 📊 Добавление тестовых данных

### Автоматическое создание данных

```bash
python manage.py loaddata fixtures/initial_data.json
```

### Ручное добавление данных

1. Войдите в админ-панель: `http://localhost:8000/admin/`
2. Добавьте категории товаров
3. Добавьте бренды
4. Добавьте товары с изображениями
5. Создайте тестовых пользователей

## 🚀 Запуск проекта

### Режим разработки

```bash
python manage.py runserver 0.0.0.0:8000
```

Проект будет доступен по адресу: `http://localhost:8000`

### Режим продакшена

1. Установите Gunicorn:

```bash
pip install gunicorn
```

2. Запустите через Gunicorn:

```bash
gunicorn phone_shop.wsgi:application --bind 0.0.0.0:8000
```

## 🎛️ Административная панель

Админ-панель доступна по адресу: `http://localhost:8000/admin/`

### Основные разделы:

- **Аутентификация и авторизация**: Управление пользователями и группами
- **Каталог**: Товары, категории, бренды, отзывы
- **Заказы**: Управление заказами и статусами
- **Корзина**: Просмотр корзин пользователей
- **Блог**: Управление статьями и новостями
- **Основное**: Настройки сайта и SEO

## 🔧 Использование

### Для покупателей:

1. **Регистрация/Вход**: Создайте аккаунт или войдите в существующий
2. **Просмотр каталога**: Просматривайте товары, используйте фильтры и поиск
3. **Добавление в корзину**: Добавляйте товары в корзину
4. **Оформление заказа**: Выберите способ доставки и оплаты
5. **Отслеживание заказов**: Просматривайте статус заказов в личном кабинете

### Для администраторов:

1. **Управление товарами**: Добавляйте, редактируйте и удаляйте товары
2. **Обработка заказов**: Просматривайте и обновляйте статусы заказов
3. **Управление пользователями**: Просматривайте профили пользователей
4. **Аналитика**: Просматривайте отчеты о продажах

## 📞 Техническая поддержка

### Контакты

- **Email**: support@phoneshop.com
- **Документация**: https://docs.phoneshop.com

---

**PhoneShop v1.0** - Готов к использованию